import React from 'react';
import styled from 'styled-components';

const VirtualRoom: React.FC = () => {
  return <S.RoomWrapper></S.RoomWrapper>;
};
const S = {
  RoomWrapper: styled.div``,
};
export default VirtualRoom;
